---
  - hosts: localhost
    gather_facts: false
    tasks:
      - name: Update deploy
        k8s:
          state: present
          definition: "{{ lookup('file', '{{baseDir}}/helloworld-k8s.yml') | replace ('$VERSION$',hostvars['version']) }}"
          namespace: default